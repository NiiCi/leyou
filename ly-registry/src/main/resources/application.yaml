server:
  port: 8761
spring:
  application:
    name: eureka-register
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defalutZone: http://127.0.0.1:${server.port}/eureka
  server:
    enable-self-preservation: false #关闭自我保护
